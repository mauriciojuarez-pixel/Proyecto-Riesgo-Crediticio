<!-- frontend/src/views/dashboard.ejs -->

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Dashboard - Riesgo Crediticio</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <nav>
        <% const currentUser = typeof user !== 'undefined' ? user : null; %>
        <span>Bienvenido, <%= currentUser ? currentUser.username : "Invitado" %></span>
        <a href="/auth/logout">Cerrar Sesión</a>
    </nav>

    <div class="container">
        <h1>Dashboard</h1>

        <% if (error) { %>
            <div class="alert alert-danger"><%= error %></div>
        <% } %>

        <p>Bienvenido al panel principal del sistema de evaluación de riesgo crediticio.</p>

        <div class="links">
            <a href="/dashboard">Evaluar Riesgo</a><br>
            <a href="/admin/usuarios">Administración de Usuarios</a>
        </div>
    </div>
</body>
</html>
